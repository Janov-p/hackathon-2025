import jsonLoader
import os, sys

def extractDataOneNext(lst:list, fstArg:str, sndArg:str):
  """_summary_

  Args:
      lst (list): fichier json
      fstArg (str): premierArgument
      sndArg (str): 2ndArgument

  Returns:
      _type_: Something
  """
  return lst[fstArg][sndArg]

def extractDataKNb(lst:list):
  return

